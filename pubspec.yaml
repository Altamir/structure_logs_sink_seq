name: structure_logs_sink_seq
description: Sink Seq Log
version: 0.0.2
homepage: https://github.com/Altamir/structure_logs_sink_seq

environment:
  sdk: '>=3.1.5 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  structured_logger: ^0.0.2
  http: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
 


flutter:

